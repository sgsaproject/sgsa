<?php

/**
 * Inserindo valores padrões
 */
echo 'Inserindo dados padrões' . BREAK_LINE;




if (APPLICATION_ENV == 'development' || APPLICATION_ENV == 'staging' || APPLICATION_ENV == 'production'):

endif;

if (APPLICATION_ENV == 'testing'):
    
endif;